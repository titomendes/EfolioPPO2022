#pragma once
#include<iostream>
#include<string>
using namespace std;
class Auxiliar
{
public:
	int inputIntUtilizador();
	float inputFloatUtilizador();
	bool verificaTeste(string subUrl);
	bool verificaTeste2(string subUrl);
	bool checkUrl(string url);
	//ordenar e verificalao de carateres e coordenadas entre 0 e 1
};

